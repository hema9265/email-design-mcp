export const LAYOUT_RULES = `### Layout Principles
- Content width: 600px maximum (email standard)
- Single column preferred for mobile readability
- If using multi-column: max 3 columns, they stack on mobile
- Visual hierarchy: most important content first (inverted pyramid)
- Hero section: large, attention-grabbing, above the fold
- Footer: always include — company info, unsubscribe link, address
- White space: generous — don't crowd content
- Section backgrounds: alternate between white and light gray for visual separation
- Images: max 600px wide, always include alt text, use placeholder URLs if none provided`;
